# Agent-Zs 依赖

# Web 框架
fastapi==0.115.0
uvicorn[standard]==0.30.0
pydantic-settings==2.5.0
python-multipart==0.0.9

# 数据库
sqlalchemy[asyncio]==2.0.35
aiomysql==0.2.0
pymysql==1.1.1

# LLM
anthropic==0.39.0
httpx==0.27.0

# 编排框架（LangGraph 图编排）
langgraph>=0.2.0

# 加密（配置中心敏感字段 Fernet 加密 + 密码验证 + JWT）
cryptography>=42.0.0
passlib[bcrypt]>=1.7.4
PyJWT>=2.8.0

# 向量数据库
pymilvus>=2.4.0

# 工具
python-dotenv==1.0.1
sse-starlette==2.1.0
redis==5.0.0

# 定时任务调度
APScheduler==3.10.4

# 测试
pytest==8.3.0
pytest-asyncio==0.24.0
httpx==0.27.0
